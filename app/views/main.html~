<!DOCTYPE html>

<html>
    <head>
        <title>#{get 'title' /}</title>
        <meta charset="${_response_encoding}">
	<link rel="stylesheet" media="screen" href="@{'/resource/stylesheets/bootstrap.min.css'}">
        <link rel="stylesheet" media="screen" href="@{'/resource/stylesheets/main.css'}">
        #{get 'moreStyles' /}
        <link rel="shortcut icon" type="image/png" href="@{'/resource/images/favicon.png'}">
        <script src="@{'/resource/javascripts/jquery-1.6.4.min.js'}" type="text/javascript" charset="${_response_encoding}"></script>
        #{get 'moreScripts' /}
    </head>
    <body>
		<div class="topbar drop-shadow">
			<h1>ProjectManager</h1>
			<ul>
				<!--<li>${session.get("username")}</li>-->
				#{if session.get("user")}
					<li class="btn"><a href="@{Personal.index}">Home</a></li>
					<li class="btn"><a href="@{Personal.projects}">Projects</a></li>
					<li class="btn"><a href="@{Personal.logout}">Logout</a></li>
				#{/if}			
				#{else}
					<li class="btn"><a href="@{Public.index}">Home</a></li>
					<li class="btn"><a href="@{Public.login}">Login</a></li>
				#{/else}
			</ul>
			<div class="clear"></div>
		</div>
		<div class="">
		    	#{doLayout /}
		</div>
    </body>
</html>
